{
  "buildCommand": "npm run build -w @smc/web",
  "outputDirectory": "apps/web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/health", "destination": "/api/auth?route=session&health=1" },
    { "source": "/api/auth/:path*", "destination": "/api/auth?route=:path*" }
  ]
}
