# Production dashboard environment variables (configure these in Vercel).
VITE_API_BASE_URL=https://smc-trader-api.<your-account>.workers.dev
# Leave blank while the Worker uses HTTP polling rather than WebSockets.
VITE_WS_BASE_URL=
